{{define "main"}}
    <div class="content">
        <h3>Game {{.Game.ID}}</h3>
        <h1>{{.Game.Title}}</h1>

        <img src={{.LogoUrl}}
             class="curation-image" alt="curation image">
        <img src={{.ScreenshotUrl}}
             class="curation-image" alt="curation image">
    </div>
{{end}}G